import { MailmanPacket } from "../packet/types";
export type MiddlewareFn = (packet: MailmanPacket, next: () => Promise<MailmanPacket>) => Promise<MailmanPacket>;
/**
 * Compose an array of middleware functions around a core dispatch fn.
 * Middleware runs in insertion order (first `use()` call runs outermost).
 */
export declare function composeMiddleware(middleware: MiddlewareFn[], core: (packet: MailmanPacket) => Promise<MailmanPacket>): (packet: MailmanPacket) => Promise<MailmanPacket>;
//# sourceMappingURL=middleware.d.ts.map