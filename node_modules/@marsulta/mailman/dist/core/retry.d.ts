export interface RetryPolicy {
    /** Max total attempts (including the first). Default: 3 */
    maxAttempts: number;
    /** Base delay in ms for the first retry. Default: 250 */
    baseDelayMs: number;
    /** Hard cap on computed delay. Default: 5000 */
    maxDelayMs: number;
    /** Backoff strategy. Default: 'exponential' */
    backoff: "exponential" | "fixed";
}
export declare const DEFAULT_RETRY_POLICY: RetryPolicy;
export type RetrySuccess<T> = {
    ok: true;
    result: T;
    attempts: number;
};
export type RetryFailure = {
    ok: false;
    error: Error;
    attempts: number;
};
export type RetryOutcome<T> = RetrySuccess<T> | RetryFailure;
export declare function withRetry<T>(fn: () => Promise<T>, policy?: RetryPolicy): Promise<RetryOutcome<T>>;
//# sourceMappingURL=retry.d.ts.map