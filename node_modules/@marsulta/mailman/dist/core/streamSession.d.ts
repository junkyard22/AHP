import { StreamController } from "../packet/types";
export declare class StreamSession implements StreamController {
    private readonly queue;
    private readonly waiters;
    private _ended;
    get isEnded(): boolean;
    push(chunk: string): void;
    end(): void;
    error(err: Error): void;
    [Symbol.asyncIterator](): AsyncGenerator<string>;
    private enqueue;
    private dequeue;
}
export declare class StreamSessionStore {
    private readonly sessions;
    create(streamId: string): StreamSession;
    get(streamId: string): StreamSession | undefined;
    delete(streamId: string): void;
    has(streamId: string): boolean;
    activeCount(): number;
}
//# sourceMappingURL=streamSession.d.ts.map