import { MailmanPacket } from "../packet/types";
export interface DLQEntry {
    id: string;
    packet: MailmanPacket;
    error: string;
    attempts: number;
    firstSeen: string;
    lastSeen: string;
}
export declare class DeadLetterQueue {
    private readonly db;
    /**
     * @param dbPath SQLite file path. Defaults to ~/.mailman/traces.db.
     *               Pass ':memory:' for tests.
     */
    constructor(dbPath?: string);
    push(packet: MailmanPacket, error: string, attempts: number): DLQEntry;
    list(): DLQEntry[];
    get(id: string): DLQEntry | undefined;
    count(): number;
    remove(id: string): boolean;
    clear(): void;
}
//# sourceMappingURL=dlq.d.ts.map