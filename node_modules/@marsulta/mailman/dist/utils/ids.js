"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.newPacketId = newPacketId;
exports.newTaskId = newTaskId;
exports.newId = newId;
const uuid_1 = require("uuid");
/**
 * Generate a new unique packet ID.
 * Format: pkt_<uuid-short>
 */
function newPacketId() {
    return `pkt_${(0, uuid_1.v4)().replace(/-/g, "").slice(0, 16)}`;
}
/**
 * Generate a new unique task ID.
 * Format: task_<uuid-short>
 */
function newTaskId() {
    return `task_${(0, uuid_1.v4)().replace(/-/g, "").slice(0, 16)}`;
}
/**
 * Generate a generic unique ID with an optional prefix.
 */
function newId(prefix = "id") {
    return `${prefix}_${(0, uuid_1.v4)().replace(/-/g, "").slice(0, 16)}`;
}
//# sourceMappingURL=ids.js.map