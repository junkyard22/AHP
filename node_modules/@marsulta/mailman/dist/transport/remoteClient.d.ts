import { MailmanPacket, MailmanRoleRegistration } from "../packet/types";
export interface RemoteClientConfig {
    /** Base URL of the MailmanServer. Example: 'http://localhost:7337' */
    url: string;
    /** Request timeout in ms. Default: 30000 */
    timeoutMs?: number;
}
export declare class RemoteMailmanClient {
    private readonly baseUrl;
    private readonly timeoutMs;
    constructor(config: RemoteClientConfig);
    send(packet: MailmanPacket): Promise<MailmanPacket>;
    ping(roleName: string): Promise<boolean>;
    listRoles(): Promise<MailmanRoleRegistration[]>;
    health(): Promise<{
        running: boolean;
        stats: Record<string, unknown>;
        roles: string[];
    }>;
    private post;
    private get;
    private request;
}
//# sourceMappingURL=remoteClient.d.ts.map