import { AHPTestTraceStore } from "./logging";
import { AHPEnvelope, AHPEnvelopeKind, AHPTestDirection } from "./types";
type FaultDirection = Extract<AHPTestDirection, "workbench->ahp" | "ahp->workbench">;
type FaultControls = {
    disconnect: () => Promise<void>;
    reconnect: () => Promise<void>;
};
type FaultOperation = {
    direction: FaultDirection;
    envelope: AHPEnvelope;
};
export interface FaultInjectionRule {
    id: string;
    direction?: FaultDirection;
    kind?: AHPEnvelopeKind;
    taskId?: string;
    times?: number;
    occurrence?: number;
    delayMs?: number;
    jitterMaxMs?: number;
    duplicateCount?: number;
    duplicateSpacingMs?: number;
    drop?: boolean;
    disconnectBeforeDeliver?: boolean;
    reconnectAfterMs?: number;
}
export interface FaultInjectionConfig {
    seed?: number;
    rules?: FaultInjectionRule[];
}
export declare class DeterministicFaultInjector {
    private readonly traces?;
    private readonly rules;
    private readonly matchCounts;
    private rngState;
    constructor(config?: FaultInjectionConfig, traces?: AHPTestTraceStore | undefined);
    describeRules(): string[];
    transmit(operation: FaultOperation, deliver: () => Promise<void>, controls: FaultControls): Promise<void>;
    private takeMatch;
    private recordFault;
    private nextInt;
}
export {};
//# sourceMappingURL=faultInjection.d.ts.map