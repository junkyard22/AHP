import { AHPTestTraceStore } from "./logging";
import { AHPTestTransport } from "./transport";
import { AHPEnvelope, AHPTestAgentDescriptor, AHPTestEvent, AHPTestSession, AHPTestSessionConfig, AHPTestTimeouts, CanonicalApi, CanonicalTaskEnvelope } from "./types";
export declare class AHPTestBus implements CanonicalApi {
    private readonly transport;
    private readonly traces;
    private readonly subscribers;
    private readonly taskStates;
    private readonly taskSequences;
    private readonly seenMessageIds;
    private readonly registeredAgents;
    private readonly pendingSessions;
    private session?;
    private timeouts;
    constructor(transport: AHPTestTransport, traces: AHPTestTraceStore, timeouts?: Partial<AHPTestTimeouts>);
    connect(sessionConfig: AHPTestSessionConfig): Promise<AHPTestSession>;
    registerAgent(agentDescriptor: AHPTestAgentDescriptor): Promise<void>;
    sendTask(taskEnvelope: CanonicalTaskEnvelope): Promise<void>;
    cancelTask(taskId: string): Promise<void>;
    onEvent(callback: (event: AHPTestEvent) => void): () => void;
    traceTask(taskId: string): import("./types").AHPTestLogEntry[];
    allTraces(): import("./types").AHPTestLogEntry[];
    restart(): Promise<void>;
    injectEnvelope(envelope: AHPEnvelope): Promise<void>;
    recordMaestroIngress(params: {
        taskId: string;
        agentId: string;
        sessionId: string;
        correlationId: string;
        eventType: string;
        startedAt?: string;
    }): string;
    private dispatchTask;
    private receiveEnvelope;
    private emitEvent;
    private armAckTimer;
    private armInactivityTimer;
    private finishTask;
    private nextSequence;
    private recordOutbound;
    private requireSession;
    private clearTimer;
    private withTimeout;
}
//# sourceMappingURL=AHPTestBus.d.ts.map