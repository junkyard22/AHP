import { FaultInjectionConfig } from "./faultInjection";
import { AHPTestTraceStore } from "./logging";
import { AHPTestTransport, TransportEnvelopeReceiver } from "./transport";
import { AHPEnvelope, AHPTestAgentDescriptor } from "./types";
interface LocalTransportOptions {
    faultInjection?: FaultInjectionConfig;
}
export declare class LocalTransport implements AHPTestTransport {
    readonly kind: "local";
    private readonly host;
    private readonly injector;
    private receiver?;
    private server?;
    private wss?;
    private port?;
    private serverSocket?;
    private clientSocket?;
    private startPromise?;
    private clientConnectPromise?;
    constructor(options?: LocalTransportOptions, traces?: AHPTestTraceStore);
    attachReceiver(handler: TransportEnvelopeReceiver): void;
    registerAgent(agentDescriptor: AHPTestAgentDescriptor, sessionId: string): Promise<void>;
    sendEnvelope(envelope: AHPEnvelope): Promise<void>;
    disconnect(sessionId: string, agentIds: string[]): Promise<void>;
    injectEnvelope(envelope: AHPEnvelope): Promise<void>;
    close(): Promise<void>;
    private ensureStarted;
    private ensureClientConnected;
    private waitForServerSocket;
    private handleServerMessage;
    private handleClientMessage;
    private actualSendToServer;
    private sendToClient;
    private actualSendToClient;
    private closeClientSocket;
}
export {};
//# sourceMappingURL=LocalTransport.d.ts.map