"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.AHPTestTraceStore = void 0;
const time_1 = require("../../utils/time");
class AHPTestTraceStore {
    entries = [];
    record(entry) {
        const normalized = {
            ...entry,
            timestamp: entry.timestamp ?? (0, time_1.now)(),
        };
        this.entries.push(normalized);
        return normalized;
    }
    recordHop(params) {
        return this.record({
            ...params,
            latencyMs: params.startedAt ? (0, time_1.elapsedMs)(params.startedAt) : 0,
        });
    }
    all() {
        return [...this.entries];
    }
    byTaskId(taskId) {
        return this.entries.filter((entry) => entry.taskId === taskId);
    }
}
exports.AHPTestTraceStore = AHPTestTraceStore;
//# sourceMappingURL=logging.js.map